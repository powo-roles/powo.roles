---
default_playbook: playbooks/default.yml
ansible:
  virtualenv:
    selinux: true
    path: .tools/virtualenv
    requirements:
      - ansible == 11.5.0
      - ovh
    python: python3
interpreter: /bin/python3
